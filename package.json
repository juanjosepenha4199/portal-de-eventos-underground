{
  "name": "portal-de-eventos-underground",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "npm run dev --workspace=@portal/frontend",
    "build": "npm run build --workspace=@portal/frontend",
    "start": "npm run start --workspace=@portal/frontend",
    "lint": "npm run lint --workspace=@portal/frontend",
    "db:generate": "npm run db:generate --workspace=@portal/backend",
    "db:push": "npm run db:push --workspace=@portal/backend",
    "db:seed": "npm run db:seed --workspace=@portal/backend",
    "db:studio": "npm run db:studio --workspace=@portal/backend"
  }
}
